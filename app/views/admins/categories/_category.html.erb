<tr>
  <td><%= category.id %></td>
  <td><%= category.name %></td>
  <td><%= link_to t(".edit"), edit_admins_category_path(category), class: "btn btn-primary" %></td>
  <td><%= link_to t(".delete"), admins_category_path(category),
    method: :delete, data: {confirm: t(".sure")}, class: "btn btn-danger" %>
  </td>
</tr>
